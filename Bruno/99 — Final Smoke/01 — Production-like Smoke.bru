meta {
  name: 01 — Production-like Smoke
  type: http
  seq: 1
}

get {
  url: {{base_url}}/health
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("Platform health is healthy", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().status).to.equal("healthy");
  });
}
