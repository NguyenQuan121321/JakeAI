meta {
  name: 01 — Missing Authentication
  type: http
  seq: 1
}

get {
  url: {{base_url}}/api/v1/finops/summary
  body: none
  auth: none
}

headers {
  Content-Type: application/json
}

tests {
  test("ATTACK: Unauthenticated financial query. EXPECTED: 401 Unauthorized", function() {
    expect(res.getStatus()).to.equal(401);
  });
}
