meta {
  name: 05 — Invalid Input
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/v1/rag/query
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_a}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "query": "",
    "top_k": -10
  }
}

tests {
  test("ATTACK: Out-of-bounds schema fuzzing. EXPECTED: 422 Unprocessable Entity", function() {
    expect(res.getStatus()).to.equal(422);
  });
}
