meta {
  name: 07 — Delete Provider Key
  type: http
  seq: 7
}

delete {
  url: {{base_url}}/api/v1/byok/keys/{{provider}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token_a}}
}

headers {
  Content-Type: application/json
}

tests {
  test("Purge returns 200 OK", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
