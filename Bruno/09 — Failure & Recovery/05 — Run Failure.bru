meta {
  name: 05 — Run Failure
  type: http
  seq: 5
}

post {
  url: {{base_url}}/api/v1/agent/tasks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token_a}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "goal": "Unexecutable task designed to test terminal failure handling",
    "metadata": {
      "forced_failure": true
    }
  }
}

tests {
  test("Task created successfully", function() {
    expect(res.getStatus()).to.equal(201);
  });
}
